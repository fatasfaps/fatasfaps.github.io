<!doctype html>
<html lang="{{ site.lang }}">
<head>
  {% include head.liquid %}
</head>

<body class="{% if site.navbar_fixed %}fixed-top-nav{% endif %} {% unless site.footer_fixed %}sticky-bottom-footer{% endunless %}">
  {% include header.liquid %}

  <div class="container mt-5" role="main">

    {% if page.title %}
      <header class="post-header mb-4"> 
        <h1 class="page-title font-weight-bold">{{ page.title }}</h1>
      
        {% if page.subtitle %}
          <p class="page-subtitle font-weight-bold" style="color: var(--global-theme-color) !important; font-weight: 800 !important; font-size: 1.1rem; margin-top: -10px;">
            {{ page.subtitle }}
          </p>
        {% endif %}

        {% if page.description %}
          <p class="post-description">
            {{ page.description | markdownify }}
          </p>
        {% endif %}
        <hr>
      </header>
    {% endif %}

    {{ content }}

  </div>

  {% include footer.liquid %}
  {% include scripts.liquid %}
</body>
</html>
